#include <stdio.h>
#include <string.h>
#include <gcrypt.h>

void hmac_SHA256(char *, int);

double calculateMedian(double [],int );

char * randomKey(int );

void hmac_sha1(char *, int);

void aes256(char *, int);

void aes128(char *, int);

void hmac_MD5(char *, int);
